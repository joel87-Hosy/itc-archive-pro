{
  "name": "itc-archive-workspace",
  "private": true,
  "scripts": {
    "local": "powershell -ExecutionPolicy Bypass -File ./scripts/start-local.ps1",
    "publish": "powershell -ExecutionPolicy Bypass -File ./scripts/publish-validated.ps1",
    "backend": "npm --prefix backend run dev",
    "frontend": "npm --prefix frontend start",
    "build": "npm --prefix frontend run build",
    "deploy": "npm --prefix frontend run deploy"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
